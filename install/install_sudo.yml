---
- name: Install sudo
  hosts: all
  become_method: su
  become: yes
  vars_files:
    - ../vars/vault.yml
  tasks:
    - name: Install sudo
      apt:
        name: sudo
    - name: Add user to sudo group
      user:
        name: "{{ ansible_user }}"
        groups: sudo
        append: yes